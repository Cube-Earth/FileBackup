plugins {
    id 'eclipse'
    id 'java'
    id 'distribution'
    id 'java-library'
}

repositories {
    mavenCentral()
}

java.toolchain.languageVersion.set(JavaLanguageVersion.of(8))


test {
    useJUnitPlatform()
    
    testLogging {
        events 'PASSED', 'FAILED', 'SKIPPED'
    }    

}

task srcZip(type: Zip) {
	archiveClassifier.set('src')
    from projectDir
    include 'src/**/*'
    include 'test-data/**/*'
    include '*.gradle'
}

dependencies {
	testImplementation project(":ce-assertions")

	annotationProcessor("org.projectlombok:lombok:1.18.28")
	compileOnly group: 'org.projectlombok', name: 'lombok', version: '1.18.28'

	implementation 'com.googlecode.plist:dd-plist:1.27'

	implementation group: 'org.apache.commons', name: 'commons-configuration2', version: '2.8.0'
	implementation group: 'commons-beanutils', name: 'commons-beanutils', version: '1.9.4'
	implementation group: 'org.xerial', name: 'sqlite-jdbc', version: '3.40.0.0'
	implementation 'commons-cli:commons-cli:1.5.0'

	implementation group: 'org.apache.logging.log4j', name: 'log4j-api', version: '2.17.2'
  	implementation group: 'org.apache.logging.log4j', name: 'log4j-core', version: '2.17.2'
  	implementation group: 'org.apache.logging.log4j', name: 'log4j-slf4j-impl', version: '2.17.2'

	implementation 'javax.mail:javax.mail-api:1.6.2'


    testImplementation 'org.junit.jupiter:junit-jupiter-api:5.9.1'
    testImplementation 'org.junit.jupiter:junit-jupiter-params:5.9.1'
	testImplementation group: 'org.hamcrest', name: 'hamcrest', version: '2.2'
    
    testRuntimeOnly 'org.junit.jupiter:junit-jupiter-engine:5.9.1'
 }
 

ext.mainClassName='earth.cube.tools.file_backup.Application'

jar {
  manifest {
    attributes(
      'Class-Path': configurations.compileClasspath.collect { it.getName() }.join(' '),
      'Main-Class': "${{ -> project.hasProperty('mainClassName') ? mainClassName : '' }}",
      'buildTime': new java.text.SimpleDateFormat("yyyy-MM-dd'T'HH:mm:ss.SSSZ").format(new java.util.Date())
    )
  }
} 
 
distributions {
    main {
		distributionBaseName = "${project.name}"

    	contents {

            into('/') {
 				from { jar }
					filePermissions {
			        	unix(0750)
				    }					
 				from { project(':FileBackup').configurations.runtimeClasspath }
					filePermissions {
			        	unix(0750)
				    }					
 				from { project(':FileBackup').sourceSets.main.resources }
					filePermissions {
			        	unix(0750)
				    }					
            }

		}
	}
}